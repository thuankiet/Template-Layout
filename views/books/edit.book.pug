extends ../layout/common

block content
  
  .container
    h1 Edit book
    
    form(action='/books/edit/'+book.id, method='POST', class='d-flex')
      input(name='title', type='text', value=book.title, class='form-control')
      button(type='submit' class='btn btn-primary ml-2') Update