extends ../layout/common

block content

  .container
    h1 Edit User

    form(action='/users/edit-user/'+user.id, method='POST', class='d-flex')
      input(name='userName', type='text', value=user.userName, class='form-control')
      button(type='submit', class='btn btn-primary ml-2') Update